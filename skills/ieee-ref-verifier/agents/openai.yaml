interface:
  display_name: "IEEE Reference Verifier"
  short_description: "Verify references field by field"
  default_prompt: "Use $ieee-ref-verifier to verify every reference against DOI and official sources and return corrections separately."
