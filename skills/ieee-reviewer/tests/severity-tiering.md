# Severity-tiering behavior fixture

A multi-robot controller claims safe generalization to unseen dynamic scenes, but reports only one simulator map and no hardware or out-of-distribution test. A figure legend omits the uncertainty definition, and the abstract uses an undefined acronym.

Expected: classify the unsupported central generalization/safety claim as Major and Blocking under `deployment-realism`, `experimental-design`, or `claim-moderation`; allow decisive validation or claim narrowing as the resolution test. Keep the legend and acronym defects Minor. Do not change severity because one repair is easier to write. Do not invent hardware tests, scenes, metrics, locations, or reviewer specialties.
