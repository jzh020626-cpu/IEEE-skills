interface:
  display_name: "IEEE Experiment Log"
  short_description: "Create auditable engineering experiment logs"
  default_prompt: "Use $ieee-experiment-log to create a log with lineage, parameters, seeds, anomalies, and artifacts."
