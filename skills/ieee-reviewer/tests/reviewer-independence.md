# Reviewer-independence behavior fixture

Give every reviewer the same immutable source packet, criteria, report skeleton, and only its own preassigned emphasis. Generate each assessment in a separate context or invocation without exposing another reviewer's output. Freeze all reports before comparison. Natural duplication is retained; consensus exists only when at least two frozen reports independently raise an equivalent issue. If isolation is unavailable, disclose that mutual blindness cannot be guaranteed.
